/**
 * Admin formlar uchun qulay re-export.
 * toDataUrl o'rniga: const { url } = await uploadToR2(file, "banners");
 */
export {
  uploadToR2,
  uploadToR2Url,
  UPLOAD_FOLDERS,
} from "../services/uploadApi";
